# Hyperion issuer-relay routing — DEMO / mock config (stand-in for Thredd).
# This is the file the Remediation Agent edits during a fix (active_relay_path).
active_relay_path: secondary

paths:
  primary:
    host: relay-primary.internal
    timeout_ms: 4000
  secondary:
    host: relay-secondary.internal
    timeout_ms: 4000

issuers:
  BBT: { name: "Bybit" }
  RVL: { name: "Client B" }
  NXP: { name: "Client C" }
